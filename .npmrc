# Expo SDK 54 + npm: web-зависимости (react-dom) подтягиваются с более новым
# react, чем зафиксированный SDK-ом react@19.1.0. Нативное приложение от этого
# не страдает, поэтому ослабляем строгую проверку peer-зависимостей npm.
legacy-peer-deps=true
